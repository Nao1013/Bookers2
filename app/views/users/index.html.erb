<div class="container">
  <div class="row">
  <div class="col-4">
 <%= render 'user_list', users: @users %>
      
      <div class="col-8 mt-3">
        <h2>Users</h2>
        <tacle class="table">
          <thead>
            <tr>
              <th scope="col">image</th>
              <th scope="col">name</th>
              <th scope="col"></th>
            </tr>
          </thead>
          <tbody>
            <% @users.each do |post_image| %>
            <tr>
              <%= image_tag @users.get_image %>
              <th><%= image_tag 'no_images.jpg' %></th>
              <th><%= book.user_name %></th>
              <th></th>
            <% end %>
            </tr>
          </tbody>
        </tacle>
      </div>
    </div>
    </div>
    </div>